# SonarQube Cloud Automatic Analysis reads this file and ignores
# sonar-project.properties, which remains available for CI-based scans.
sonar.python.version=3.12
sonar.sourceEncoding=UTF-8

sonar.sources=backend/app,backend/main.py,backend/Dockerfile,frontend/src,.github/workflows,supabase/migrations
sonar.tests=backend/tests
sonar.test.inclusions=backend/tests/**/*.py
sonar.exclusions=**/.venv/**,**/node_modules/**,**/dist/**,**/__pycache__/**

# Historical migrations remain security-scanned but are excluded from
# copy-paste metrics because they are immutable cumulative snapshots.
sonar.cpd.exclusions=supabase/migrations/**
