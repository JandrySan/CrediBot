class EmbeddingService:
    """Placeholder para una futura integracion con pgvector o embeddings externos."""

    def embed_text(self, text: str) -> list[float]:
        raise NotImplementedError("EmbeddingService queda reservado para una fase futura.")
